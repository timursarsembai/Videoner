-- Отметка о единственной подсказке, которую бот отправляет человеку, если тот
-- запустил его и за 48 часов не прислал ни одной ссылки. NULL = ещё не писали,
-- дата = писали (повторно не пишем никогда, даже спустя месяцы).
ALTER TABLE "BotUser" ADD COLUMN "nudgeSentAt" TIMESTAMP(3);
